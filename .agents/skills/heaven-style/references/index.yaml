# Generated by scripts/index.py. Do not edit.
schema: heaven-style-index/v2
skill:
  name: heaven-style
  version: 0.1.2.15
  description: Standalone personal code-quality, architecture, documentation, graphical-interface, and project-management
    guide for TypeScript and Python repositories. Use when writing or reviewing code or docs, designing architecture or UI,
    refactoring, or aligning tests; apply repository policy first, then shared design philosophy and the matched language
    rules.
  source_digest: sha256:a7bf93968806470980fef7ddee4044c7a6ac70d55a857beca8d50e2c9180bc3c
entrypoints:
  default: SKILL.md
  start: references/workflows/start.md
  rules: references/rules/overview.md
  architect: references/workflows/architect.md
  editor: references/workflows/editor.md
counts:
  rules: 33
  examples: 2
  design: 1
  workflows: 5
  tasks: 10
  failures: 4
routes:
  rules:
    overview:
      path: references/rules/overview.md
      use: Use when deciding which heaven-style rule files apply to a task.
      family: overview
      blocking: true
    ts-util:
      path: references/rules/code/typescript/util.md
      use: Use when TypeScript code touches files, paths, resources, serialization, logging, hashing, IDs, temp data, subprocesses,
        or shared utility ownership.
      family: code-quality
      blocking: true
    ts-architecture:
      path: references/rules/code/typescript/architecture.md
      use: Use when designing or reviewing TypeScript modules, public APIs, services, adapters, providers, registries, strategies,
        capabilities, or lifecycle boundaries.
      family: code-quality
      blocking: true
    ts-api:
      path: references/rules/code/typescript/api.md
      use: Use when designing TypeScript public classes/functions, naming methods, choosing class versus function, shaping
        fluent or collection APIs, or reviewing code readability and helper cost.
      family: code-quality
      blocking: true
    ts-types:
      path: references/rules/code/typescript/types.md
      use: Use when adding or reviewing TypeScript types, public signatures, external data, unions, identifiers, config shapes,
        assertions, or strict compiler options.
      family: code-quality
      blocking: true
    ts-config:
      path: references/rules/code/typescript/config.md
      use: Use when TypeScript code reads, resolves, edits, persists, watches, versions, scopes, authorizes, or exposes configuration
        through JSON, YAML, files, memory, databases, Redis, or another backend.
      family: code-quality
      blocking: true
    ts-modules:
      path: references/rules/code/typescript/modules.md
      use: Use when adding or reviewing TypeScript files, package boundaries, imports, exports, workspaces, barrels, optional
        integrations, build output, or published artifacts.
      family: code-quality
      blocking: true
    ts-async:
      path: references/rules/code/typescript/async.md
      use: Use when TypeScript code handles promises, callbacks, events, cancellation, errors, subprocesses, resources, concurrency,
        startup, or teardown.
      family: code-quality
      blocking: true
    ts-sql:
      path: references/rules/code/typescript/sql.md
      use: Use when TypeScript code touches SQL, database clients, query builders, ORMs, migrations, transactions, pools,
        raw queries, or persisted row shapes.
      family: code-quality
      blocking: true
    ts-docs:
      path: references/rules/code/typescript/docs.md
      use: Use when adding or reviewing TypeScript public exports, package entry points, TSDoc, implementation comments, examples,
        generated catalogs, or declaration-facing documentation.
      family: code-quality
      blocking: true
    ts-compat:
      path: references/rules/code/typescript/compat.md
      use: Use when TypeScript code renames exports or package subpaths, changes config/wire/persistence schemas, adds deprecations
        or aliases, or changes supported runtimes and consumers.
      family: code-quality
      blocking: true
    ts-environment:
      path: references/rules/code/typescript/environment.md
      use: Use when creating or changing a TypeScript repository, package manager, runtime, lockfile, compiler configuration,
        scripts, linting, testing, dependency policy, workspace, package exports, or CI commands.
      family: code-quality
      blocking: true
    util:
      path: references/rules/code/python/util.md
      use: Use when Python code touches paths, files, serialization, shell commands, hashing, logging, temp files, IDs, or
        shared utility ownership.
      family: code-quality
      blocking: true
    config:
      path: references/rules/code/python/config.md
      use: Use when Python code introduces defaults, tunables, prompt or resource paths, model/backend settings, or disputed
        literals.
      family: code-quality
      blocking: true
    types:
      path: references/rules/code/python/types.md
      use: Use when adding or reviewing type annotations, public signatures, Python-version compatibility, or schema-shaped
        data.
      family: code-quality
      blocking: true
    docstring:
      path: references/rules/code/python/docstring.md
      use: Use when adding or reviewing public API docstrings, major feature APIs, generator docs, return docs, examples,
        warnings, or Markdown markup inside Python docstrings.
      family: code-quality
      blocking: true
    oop:
      path: references/rules/code/python/oop.md
      use: Use when designing public objects, stores, clients, CRUD/KV APIs, batch APIs, engines, specs, configs, plans, or
        presets.
      family: code-quality
      blocking: true
    model:
      path: references/rules/code/python/model.md
      use: Use when adding public classes or functions, designing a user-facing surface, choosing method versus free function,
        adding constructor flags, or proposing a DSL.
      family: code-quality
      blocking: true
    solid:
      path: references/rules/code/python/solid.md
      use: Use when designing or reviewing SOLID boundaries, class boundaries, extension points, inheritance contracts, adapter
        interfaces, registries, or strategy/provider dependencies.
      family: code-quality
      blocking: true
    name:
      path: references/rules/code/python/name.md
      use: Use when adding or reviewing modules, public symbols, abbreviations, predicates, variable names, or glossary terms.
      family: code-quality
      blocking: true
    files:
      path: references/rules/code/python/files.md
      use: Use when adding or reviewing Python files, package folders, public exports, lazy exports, local helper modules,
        internal helper modules, typing stubs, implementation families, adapters, providers, or feature-local module boundaries.
      family: code-quality
      blocking: true
    py:
      path: references/rules/code/python/py.md
      use: Use when code shape is verbose, deeply nested, unclear, or depends on fallback/control-flow semantics.
      family: code-quality
      blocking: true
    clean:
      path: references/rules/code/python/clean.md
      use: Use when adding Python helper functions, wrappers, temporary transforms, adapters, or deciding inline logic versus
        shared utilities.
      family: code-quality
      blocking: true
    error:
      path: references/rules/code/python/error.md
      use: Use when validating supported values, handling exceptions, logging failures, catching broadly, or returning fallbacks.
      family: code-quality
      blocking: true
    sql:
      path: references/rules/code/python/sql.md
      use: Use when Python code touches database queries, raw SQL, migrations, DDL, binds, SQLAlchemy, ORM, or SQL resources.
      family: code-quality
      blocking: true
    compat:
      path: references/rules/code/python/compat.md
      use: Use when renaming APIs, changing config schemas, adding aliases, preserving old behavior, or proposing migration/deprecation
        shims.
      family: code-quality
      blocking: true
    environment:
      path: references/rules/project/environment.md
      use: Use when configuring repository agent instructions, running shell commands, choosing a repository environment,
        adding package scripts or wrappers, or diagnosing wrong-runtime and wrong-environment failures.
      family: project
      blocking: true
    format:
      path: references/rules/project/format.md
      use: Use when running or reviewing TypeScript or Python formatters, linters, import order, generated compatibility files,
        or repository command entry points.
      family: project
      blocking: true
    test:
      path: references/rules/project/test.md
      use: Use when adding or reviewing TypeScript or Python behavior tests, runnable examples, type/contract/property checks,
        package smoke tests, integration evidence, or provider tests.
      family: project
      blocking: true
    docs:
      path: references/rules/project/docs.md
      use: Use when creating, restructuring, reviewing, or syncing authored documentation, or when work changes public behavior,
        architecture, task state, generated artifacts, examples, documentation authority, or temporary notes.
      family: project
      blocking: true
    extension:
      path: references/rules/project/extension.md
      use: Use when adding or reviewing open extension families, plugin contracts, registries, manifests, discovery/load paths,
        providers, backends, handlers, strategies, capabilities, or bundled/external parity.
      family: project
    interfaces:
      path: references/rules/project/interfaces.md
      use: Use when a package exposes an SDK, application service, API, CLI, GUI, MCP, TUI, dashboard, desktop app, or independently
        consumed transport.
      family: project
      blocking: true
    review:
      path: references/rules/project/review.md
      use: Use for PR, diff, module, branch, or Linear review, severity-marked findings, review artifacts, follow-up, or explicit
        waivers.
      family: project
      blocking: true
  examples:
    example-open-capability-vocabulary:
      path: references/examples/code/open-capability-vocabulary.md
      use: Read when an extensible family keeps gaining hard-coded feature fields, feature-specific methods, switches, or
        unvalidated extra dictionaries.
    example-local-gui-layout:
      path: references/examples/code/local-gui-layout.md
      use: Read when placing a GUI beside a service, choosing a cohesive package versus workspace packages, or wiring UI,
        host, transport, application, and domain dependencies.
  design:
    gui-style:
      path: references/design/gui-style.md
      use: Use when defining, creating, maintaining, refactoring, transferring, demonstrating, or reviewing GUI/UX design
        in any framework, including existing interfaces, app shells, dashboards, desktop tools, demos, and temporary HTML.
      default: false
  workflows:
    workflow-start:
      path: references/workflows/start.md
      use: Use when a user, developer, architect, or agent must select, start, hand off, or resume a Heaven Style workflow.
      audience: all
    workflow-work-types:
      path: references/workflows/work-types.md
      use: Use when a person or agent needs the complete start, resume, evidence, verification, communication, and closeout
        procedure for a common work type.
      audience: all
    workflow-architect:
      path: references/workflows/architect.md
      use: Use when organizing docs, designing or reviewing architecture, drafting refactor plans, updating goals, or producing
        step-by-step execution plans without implementing code.
      audience: architect
    workflow-developer:
      path: references/workflows/developer.md
      use: Use when planning broad work, refactoring shared surfaces, designing public APIs, resolving rule tradeoffs, or
        needing the full Heaven Style rule set.
      audience: developer
    workflow-editor:
      path: references/workflows/editor.md
      use: Use when maintaining this standalone heaven-style skill, changing rules/tasks/failures/workflows, regenerating
        indexes, installing mirrors, or bumping its independent version.
      audience: editor
  tasks:
    code:
      path: references/tasks/code.md
      use: Use when implementing Heaven-style features, fixes, refactors, tests, docs, or stabilization work.
    code-review:
      path: references/tasks/code-review.md
      use: Use when reviewing code changes, modules, branches, PRs, recent diffs, or Linear-linked review work.
    doc-sync:
      path: references/tasks/doc-sync.md
      use: Use when creating, restructuring, reviewing, auditing, or syncing canonical authored documentation, generated pages
        or contracts, navigation, examples, or declared sibling projections.
    doc-trans:
      path: references/tasks/doc-trans.md
      use: Use when translating or re-translating Markdown or MDX into YAML-frontmatter, line-aligned English and Simplified
        Chinese pairs in the repository's declared layout.
    test-compress:
      path: references/tasks/test-compress.md
      use: Use when auditing and slimming a TypeScript or Python test suite by removing oddly specific, legacy, low-value,
        brittle, or redundant tests while preserving meaningful behavior coverage.
    code-explain:
      path: references/tasks/code-explain.md
      use: Use when explaining TypeScript/Python repository architecture, modules, data flow, feature behavior, code changes,
        or project-specific systems.
    env:
      path: references/tasks/env.md
      use: Use when maintaining a developer machine environment, updating setup plans, performing scoped machine maintenance,
        or preparing safe local environment commands.
    arch-design:
      path: references/tasks/arch-design.md
      use: Use when designing or reviewing architecture, module boundaries, dependency direction, extension seams, refactor
        plans, or periodic architecture health using agile design principles before implementation.
    skill-update:
      path: references/tasks/skill-update.md
      use: Use when maintaining heaven-style TypeScript or Python rules, tasks, workflows, scripts, indexes, versions, evidence,
        or failure patterns.
    manager:
      path: references/tasks/manager.md
      use: Use when tracking GitHub/Linear status, stale work, recent activity, next steps, or multi-agent coordination.
  failures:
    failure-env:
      path: references/failures/env.md
      use: Use when commands fail because the wrong TypeScript/JavaScript runtime, package manager, shell, Python/uv environment,
        dependency install, or PATH is active.
    failure-network-proxy:
      path: references/failures/network-proxy.md
      use: Use when CLI/API/network commands fail while local VPN or proxy settings may be active.
    failure-auth-secrets:
      path: references/failures/auth-secrets.md
      use: Use when MCP tools, CLIs, Linear/Tavily, GraphQL, or LLM providers fail because auth, tokens, or API keys are missing
        or expired.
    failure-linear-pressure:
      path: references/failures/linear-pressure.md
      use: Use when Linear cannot create more issues, a project has too many Done issues, or an issue has too many routine
        comments.
scripts:
  index:
    path: scripts/index.py
    use: Validate heaven-style metadata and generate its compact routing index.
  install:
    path: scripts/install.py
    use: Install the standalone heaven-style skill and optional harness bridges.
  machine:
    path: scripts/machine.py
    use: Generate sanitized local-machine instance notes.
  scan:
    path: scripts/scan.py
    use: Check maintenance scripts for syntax and undeclared project dependencies.
assets:
  - assets/MacOS-env.md
  - assets/REFERENCE.md
  - assets/default-glossary.md
  - assets/instance
