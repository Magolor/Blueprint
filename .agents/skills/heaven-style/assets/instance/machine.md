---
name: asset-machine
description: Read when checking the source machine’s hardware and tool observations.
---

# Local macOS Machine

## Summary

This snapshot records non-secret facts about the source machine. Verify relevant observations before using them on another machine or in a later session.

## Snapshot scope

Non-secret machine observations generated by `scripts/machine.py`.
This snapshot describes the source machine only. Verify relevant facts before use.
Volatile versions, free space, displays, live env values, and secret values are intentionally omitted.

## Identity

- Computer: `Magolor-M5`; local host: `Magolor-M5`.
- Account: `magolor`; home: `/Users/magolor`; configured shell: `/bin/zsh`.
- Standard skill install target: `~/.agents/skills/heaven-style`.

## Platform

- macOS major `26` on `arm64`.
- MacBook Pro `Mac17,6`, Apple M5 Max.
- CPU: 18 (6 Super and 12 Performance); memory: 128 GB; internal SSD: 2.0 TB.
- GPU: 40 cores.

## Observed tools and setup

- Homebrew prefix: `/opt/homebrew`.
- Docker context: `desktop-linux`. This does not prove daemon availability.
- Setup checkout: `/Users/magolor/Developer/Setup`. Presence does not prove provisioning or profile binding.
- Desired tool ownership belongs to the selected setup and repository metadata.
- `python`: `/Users/magolor/miniforge3/envs/main/bin/python`.
- `python3`: `/usr/bin/python3`.
- `pip`: `/Users/magolor/miniforge3/envs/main/bin/pip`.
- `conda`: `/Users/magolor/miniforge3/condabin/conda`.
- `uv`: `/opt/homebrew/bin/uv`.
- `git`: `/opt/homebrew/bin/git`.
- `gh`: `/opt/homebrew/bin/gh`.
- `bun`: `/opt/homebrew/bin/bun`.
- `pnpm`: `/Users/magolor/.local/share/mise/shims/pnpm`.
- `node`: `/Users/magolor/.local/share/mise/shims/node`.
- `npm`: `/Users/magolor/.local/share/mise/shims/npm`.
- `mise`: `/opt/homebrew/bin/mise`.
- `rustup`: `/Users/magolor/.cargo/bin/rustup`.
- `docker`: `/usr/local/bin/docker`.
- `code`: `/usr/local/bin/code`.
- `cursor`: `/usr/local/bin/cursor`.

## Agent Notes

- Prefer repository wrappers or the declared environment for project work. Use `uv run python` in uv-managed repositories.
- Read the skill assets `MacOS-env.md` and `instance/setup.md` for setup ownership and missing-information recovery.
- Run `gh auth status`, `docker context show`, or `docker info` only when those integrations fail.
- Track this non-secret snapshot only after review. Never add secret values, tokens, serial numbers, UUIDs, private key paths, or transient state.
